<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Shiwakeya" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <% if logged_in? %>
      <nav class="bg-white shadow">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex">
              <div class="flex-shrink-0 flex items-center">
                <%= link_to "仕訳屋", root_path, class: "text-xl font-bold text-gray-900" %>
              </div>
              <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                <%= link_to "ダッシュボード", root_path,
                    class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
                <%= link_to "スプレッドシート", service_spreadsheets_path,
                    class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
                <% if admin? %>
                  <%= link_to "ユーザー権限", user_permissions_path,
                      class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
                <% end %>
              </div>
            </div>
            <div class="flex items-center space-x-4">
              <span class="text-sm text-gray-600">
                <%= current_user.email %>
                <% if admin? %>
                  <span class="ml-1 px-2 py-1 text-xs bg-purple-100 text-purple-800 rounded">管理者</span>
                <% elsif accountant? %>
                  <span class="ml-1 px-2 py-1 text-xs bg-blue-100 text-blue-800 rounded">会計担当者</span>
                <% else %>
                  <span class="ml-1 px-2 py-1 text-xs bg-gray-100 text-gray-800 rounded">メンバー</span>
                <% end %>
              </span>
              <%= link_to "ログアウト", logout_path,
                  data: { turbo_method: :delete },
                  class: "text-sm text-red-600 hover:text-red-800" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <% if flash[:notice] %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= flash[:notice] %></span>
        </div>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= flash[:alert] %></span>
        </div>
      </div>
    <% end %>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
