<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">スプレッドシート編集</h1>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <%= form_with model: @spreadsheet, local: true do |f| %>
      <% if @spreadsheet.errors.any? %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
          <h3 class="text-red-800 font-semibold mb-2">エラーが発生しました</h3>
          <ul class="list-disc list-inside text-red-700">
            <% @spreadsheet.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="space-y-6">
        <div>
          <%= f.label :spreadsheet_id, "スプレッドシートID", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= f.text_field :spreadsheet_id, 
                readonly: true,
                class: "block w-full px-3 py-2 bg-gray-100 border border-gray-300 rounded-md shadow-sm" %>
          </div>
          <p class="mt-2 text-sm text-gray-500">
            スプレッドシートIDは変更できません
          </p>
        </div>

        <div>
          <%= f.label :name, "表示名", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= f.text_field :name, 
                required: true,
                placeholder: "例: 売上管理シート",
                class: "block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
          </div>
        </div>

        <div>
          <%= f.label :description, "説明", class: "block text-sm font-medium text-gray-700" %>
          <div class="mt-1">
            <%= f.text_area :description, 
                rows: 3,
                placeholder: "このスプレッドシートの用途や管理内容を記入",
                class: "block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
          </div>
        </div>

        <div>
          <div class="flex items-center">
            <%= f.check_box :is_active, class: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" %>
            <%= f.label :is_active, "有効にする", class: "ml-2 block text-sm text-gray-900" %>
          </div>
          <p class="mt-1 text-sm text-gray-500">
            無効にすると、一覧に表示されなくなります
          </p>
        </div>
      </div>

      <div class="mt-8 bg-gray-50 rounded-lg p-4">
        <h3 class="text-sm font-semibold text-gray-700 mb-2">登録情報</h3>
        <dl class="text-sm">
          <div class="flex">
            <dt class="text-gray-500 w-32">登録日時:</dt>
            <dd class="text-gray-900"><%= @spreadsheet.created_at.strftime("%Y/%m/%d %H:%M") %></dd>
          </div>
          <div class="flex mt-1">
            <dt class="text-gray-500 w-32">最終更新:</dt>
            <dd class="text-gray-900"><%= @spreadsheet.updated_at.strftime("%Y/%m/%d %H:%M") %></dd>
          </div>
          <div class="flex mt-1">
            <dt class="text-gray-500 w-32">シート数:</dt>
            <dd class="text-gray-900"><%= @spreadsheet.sheets.count %></dd>
          </div>
        </dl>
      </div>

      <div class="mt-8 flex justify-between">
        <div>
          <%= link_to "削除", spreadsheet_path(@spreadsheet),
              data: { 
                turbo_method: :delete,
                turbo_confirm: "本当に削除しますか？関連するシートデータも全て削除されます。"
              },
              class: "px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700" %>
        </div>
        <div class="flex space-x-4">
          <%= link_to "キャンセル", spreadsheet_path(@spreadsheet), 
              class: "px-4 py-2 bg-gray-300 text-gray-700 rounded hover:bg-gray-400" %>
          <%= f.submit "更新", 
              class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700" %>
        </div>
      </div>
    <% end %>
  </div>
</div>