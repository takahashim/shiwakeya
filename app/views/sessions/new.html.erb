<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 flex items-center justify-center">
  <div class="max-w-md w-full mx-auto px-4">
    <div class="bg-white rounded-lg shadow-xl p-8">
      <div class="text-center mb-8">
        <h1 class="text-3xl font-bold text-gray-900">仕訳屋</h1>
        <p class="text-gray-600 mt-2">スプレッドシート管理システム</p>
      </div>

      <%= form_with url: login_path, local: true, class: "space-y-6" do |f| %>
        <div>
          <%= f.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700" %>
          <%= f.email_field :email, 
              required: true,
              autofocus: true,
              placeholder: "example@example.com",
              class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
        </div>

        <div>
          <%= f.submit "ログイン", 
              class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      <% end %>

      <div class="mt-6 text-center">
        <p class="text-sm text-gray-600">
          初めての方はメールアドレスを入力してログインすると<br>
          自動的にアカウントが作成されます
        </p>
      </div>

      <% if ENV['MASTER_SPREADSHEET_ID'].blank? %>
        <div class="mt-6 p-4 bg-yellow-50 rounded-md">
          <p class="text-sm text-yellow-800">
            <strong>設定が必要です:</strong><br>
            環境変数 MASTER_SPREADSHEET_ID と GOOGLE_SERVICE_ACCOUNT_JSON を設定してください
          </p>
        </div>
      <% end %>
    </div>
  </div>
</div>